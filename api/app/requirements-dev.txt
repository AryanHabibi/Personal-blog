# Dev-only tooling. Installed into the Dockerfile.dev image on top of the
# runtime deps. Keep this list short; production never sees it.
-r requirements.txt

ruff==0.8.6
ipython==8.31.0
pytest==8.3.4
httpx==0.28.1
